export function area_retangulo(){
    var base;
    var altura;
    
    base = parseFloat(prompt("qual a base:"));
    altura = parseFloat(prompt("qual a altura:"));

    alert("A área do retângulo é " + (base*altura))
};
